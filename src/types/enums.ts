export enum MessageType {
  Text = 'text',
  Image = 'image',
  Video = 'video',
  Audio = 'audio',
  GeoLocation = 'geolocation',
  File = 'file',
  Voice = 'voice',
}

export enum RecordingStatus {
  Recording = 'recording',
  Inactive = 'inactive',
  Paused = 'paused'
}

export enum FileTypes {
  Zip = 'zip',
  Pdf = 'pdf'
}